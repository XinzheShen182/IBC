export interface ConsortiumItemType {
    id: string;
    name: string;
  }

export interface OrgItemType {
    id: string;
    name: string;
  }

export interface EnvItemType {
    id: string;
    name: string;
  }